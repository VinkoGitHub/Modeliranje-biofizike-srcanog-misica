import streamlit as st
from presentation.utils import *

# Configuring the app
st.set_page_config(
    page_title="Diplomski rad",
    page_icon="🎓",
    layout="wide",
)

# Title
st.title("Reference")
st.divider()

# Body
st.text(
    """[1] Sundnes, J.; Lines, G. T.; Cai, X.; Nielsen, B. F.; Mardal, K. A.; Tveito, A.
Monographs in Computational Science and Engineering. Springer, 2006."""
)
st.text(
    """[2] The top 10 causes of death, (9.12.2020.), World Health Orga-
nization, https://www.who.int/news-room/fact-sheets/detail/
the-top-10-causes-of-death, 4.2.2024."""
)
st.text(
    """[3] Gheith, Z.; Alqam, B.; Jagana, R. A Rare Case of Pulmonary Emboli Presenting With
ST Elevation on ECG. // Cureus. Vol. 14, 9 (2022)"""
)
st.text("""[4] Biasi, N.; Tognetti, A. A computationally efficient dynamic model of human epicardial
tissue. // Plos one. Vol. 16 10 (2021)""")
st.text("""[5] """)
st.text("""[6] """)
st.text("""[7] """)
st.text("""[8] """)
